# Session Memory - Voda Tour & Event (16 July 2026)

## 🎯 Status Keseluruhan
- **Fase Saat Ini:** Phase 10 (Production Readiness & Rebranding cleanup) - **SELESAI** ✅
- **Kondisi Aplikasi:** 100% Production Ready. Integrasi reverse proxy untuk Shared VPS telah dikonfigurasi, dan sisa aset domain lama berhasil dibersihkan total.

## 🛠️ Pencapaian Sesi Ini

1. **Re-branding Domain & Kontak Masif (Clean-up):**
   - Membersihkan seluruh sisa domain lama `vodaeventorganizer.id`, `vodaevent.id`, dan `vodatour.event.id` di seluruh codebase.
   - Mengubah default email support menjadi `hello@vodatrip.id` dan username sosial media menjadi `@vodatrip` di component [Footer.astro](file:///home/famanca/voda-tour-event/apps/web/src/components/Footer.astro), [kontak.astro](file:///home/famanca/voda-tour-event/apps/web/src/pages/kontak.astro), [Layout.astro](file:///home/famanca/voda-tour-event/apps/web/src/layouts/Layout.astro), [sitemap.xml.ts](file:///home/famanca/voda-tour-event/apps/web/src/pages/sitemap.xml.ts), dan berkas dokumentasi markdown.

2. **Skema Port & Reverse Proxy Shared VPS:**
   - Karena VPS target bertipe **Shared Server** (berbagi port 80/443 dengan web milik rekan pengguna), port internal docker compose diatur untuk di-expose:
     - Astro Web: **`4322:4321`** (menggunakan port `4322` di VPS agar tidak bentrok).
     - Directus CMS: **`8055:8055`** (port default).
   - Menyediakan panduan integrasi reverse proxy Nginx utama VPS untuk melempar domain `vodatrip.id` ke port `4322` dan `api.vodatrip.id` ke port `8055`.
   - Mengabaikan container Nginx & Certbot bawaan docker compose di server produksi (menghemat RAM VPS sebesar ~50MB dan mencegah konflik binding port 80/443).

3. **Pengujian Produksi Lokal via Tunneling (LocalTunnel):**
   - Mengatasi permasalahan "Refused to connect" saat tunnel SSH remote forwarding default diblokir oleh `GatewayPorts no` di VPS.
   - Menggunakan alternatif **LocalTunnel** untuk melakukan *double tunneling* (`lt --port 4321` untuk web dan `lt --port 8055` untuk Directus) dari laptop lokal Arch WSL agar atasan/klien bisa mengakses web secara live.
   - Melakukan setup konfigurasi `PUBLIC_DIRECTUS_URL` pada `.env` agar aset gambar dan data CMS di laptop lokal tetap dapat diakses di browser klien tanpa pecah.

4. **Pembuatan Dokumentasi Komprehensif:**
   - Menulis file [README.md](file:///home/famanca/voda-tour-event/README.md) utama di root proyek yang menjelaskan struktur, arsitektur, panduan setup lokal, integrasi shared VPS, cara pengetesan tunneling, dan aturan panduan AI.

## 🚀 Panduan Deployment Selanjutnya

1. **Lakukan Pull:** Jalankan `git pull` di VPS.
2. **Setup Env:** Pastikan `/infrastructure/.env.prod` sudah terisi dengan benar (tidak mengandung URL tunneling sementara).
3. **Eksekusi Docker:**
   ```bash
   docker compose --env-file infrastructure/.env.prod -f docker-compose.prod.yml up -d postgres directus web --build
   ```
4. **Setup Nginx VPS:** Daftarkan block domain `vodatrip.id` (ke port `4322`) dan `api.vodatrip.id` (ke port `8055`) di Nginx utama VPS.

> *Memory file created by AI Assistant to preserve context for the next session.*
